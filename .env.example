# Senha do admin (igual no front e na API)
VITE_ADMIN_PASS=sua_senha_aqui

# Na Vercel: em Settings > Environment Variables, defina também:
# ADMIN_PASS = mesma senha (usada pela serverless function /api/cotacao)
# KV: já configurado pelo link do store; não precisa copiar aqui.
# Para dev local com vercel dev: KV_REST_API_URL e KV_REST_API_TOKEN (copie da Vercel)
