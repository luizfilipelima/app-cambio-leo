# Senha do admin (igual no front e na API)
VITE_ADMIN_PASS=sua_senha_aqui

# Na Vercel (Settings > Environment Variables):
# - ADMIN_PASS = mesma senha (para a API /api/cotacao)
# - Redis/KV: ao LIGAR o store ao projeto, a Vercel injeta automaticamente:
#   UPSTASH_REDIS_REST_URL e UPSTASH_REDIS_REST_TOKEN (integração Upstash)
#   OU KV_REST_API_URL e KV_REST_API_TOKEN (legado).
#   Se não aparecerem, crie manualmente com os valores do dashboard do Redis.
